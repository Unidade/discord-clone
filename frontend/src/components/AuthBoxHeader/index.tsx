import { Box, Typography } from '@mui/material'

export default function AuthBoxHeader() {
  return (
    <Box sx={{ display: 'flex', justifyContent: 'center' }}>
      <Typography variant='h4' fontWeight={900}>
        👀Discord
      </Typography>
    </Box>
  )
}
